"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.SelectivePropagator = void 0;
class SelectivePropagator {
    constructor(propagator, config = {}) {
        this.propagator = propagator;
        this.config = config;
    }
    inject(context, carrier, setter) {
        if (!this.config.injectEnabled)
            return;
        return this.propagator.inject(context, carrier, setter);
    }
    extract(context, carrier, getter) {
        if (!this.config.extractEnabled)
            return context;
        return this.propagator.extract(context, carrier, getter);
    }
    fields() {
        return this.propagator.fields();
    }
}
exports.SelectivePropagator = SelectivePropagator;
//# sourceMappingURL=SelectivePropagator.js.map