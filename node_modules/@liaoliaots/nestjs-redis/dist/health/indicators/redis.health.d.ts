import { HealthIndicator, HealthIndicatorResult } from '@nestjs/terminus';
import { RedisCheckSettings } from './redis-check-settings.interface';
/**
 * @public
 */
export declare class RedisHealthIndicator extends HealthIndicator {
    /**
     * Checks a redis/cluster connection.
     *
     * @param key - The key which will be used for the result object
     * @param options - The extra options for check
     */
    checkHealth(key: string, options: RedisCheckSettings): Promise<HealthIndicatorResult>;
}
