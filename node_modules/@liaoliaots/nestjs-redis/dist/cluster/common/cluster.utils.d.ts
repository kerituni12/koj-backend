import { Cluster } from 'ioredis';
import { ClusterClientOptions, ClusterClients } from '../interfaces';
import { ClientNamespace } from '../../interfaces';
export declare const createClient: (clientOptions: ClusterClientOptions) => Cluster;
export declare const displayReadyLog: (clients: ClusterClients) => void;
export declare const displayErrorLog: (clients: ClusterClients) => void;
export declare const quitClients: (clients: ClusterClients) => Promise<[PromiseSettledResult<ClientNamespace>, PromiseSettledResult<"OK">][]>;
