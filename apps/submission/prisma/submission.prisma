generator challenge {
    provider = "prisma-client-js"
    output   = "../../../node_modules/@koj-prisma/submission"
}

datasource db {
    provider = "postgresql"
    url      = env("POSTGRES_URL")
}
