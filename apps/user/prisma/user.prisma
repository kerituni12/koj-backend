generator client {
    provider = "prisma-client-js"
}

generator user {
    provider = "prisma-client-js"
    output   = "../../../node_modules/@koj-prisma/user"
}

datasource db {
    provider = "postgresql"
    url      = env("POSTGRES_URL")
}
