datasource db {
    provider = "sqlite"
    url      = "file:./dev.db"
}

generator client {
    provider = "prisma-client-js"
}

// model Placeholder {
//     id Int @id @default(autoincrement())
// }
